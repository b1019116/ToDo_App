import { type } from "os";

interface String_root{
    title: string;
    lang_menu: string;
    add_button: string;
    todo_head: string;
    finish_head: string;
    delete_button: string;
    change_color: string;
}

export default String_root;